// mockData.ts removed - frontend now calls backend API endpoints
// This stub exists to provide a clear error during development if someone still imports mock data.
throw new Error("mockData.ts is deprecated. The frontend now fetches data from the backend API. Remove imports of '@/lib/mockData' and use '@/lib/api' endpoints instead.");
